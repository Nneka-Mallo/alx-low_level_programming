#include "main.h"

/*
  *to write an isupper function
  *which returns 1 when c is uppercase
  * returns 0 otherwise
  */

int _isupper(int c);
{

	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);


}
